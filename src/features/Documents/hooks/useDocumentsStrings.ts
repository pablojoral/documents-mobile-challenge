/**
 * Static copy for the Documents screen. (i18n was removed from the project, so
 * these are plain literals; the hook keeps the strings isolated from logic per
 * the components rules.)
 */
export const useDocumentsStrings = () => {
  return {
    title: 'Documents',
  };
};
