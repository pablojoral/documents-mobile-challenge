/** Copy for the documents empty state. */
export const useDocumentsEmptyStateStrings = () => {
  return {
    title: 'No documents yet',
    subtitle: 'Pull down to refresh and load documents.',
  };
};
