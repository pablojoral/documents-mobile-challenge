/** Copy for the documents error state. */
export const useDocumentsErrorStrings = () => {
  return {
    title: 'Something went wrong',
    subtitle: "We couldn't load documents. Check that the server is running.",
    retry: 'Try again',
  };
};
