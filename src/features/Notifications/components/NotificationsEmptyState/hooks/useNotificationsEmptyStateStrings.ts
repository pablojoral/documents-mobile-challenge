/** Copy for the notifications empty state. */
export const useNotificationsEmptyStateStrings = () => {
  return {
    title: 'No notifications yet',
    subtitle: "You'll see new documents created by others here.",
  };
};
